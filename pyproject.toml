[project]
name = "basicrealtimeobjectdetection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "transformers[torch]>=4.40.0",
    "torch>=2.2.0",
    "Pillow>=10.0.0",
    "accelerate>=0.28.0",
    "fastapi[standard]>=0.141.1",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
]
